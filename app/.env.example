# Database (local Docker; use Neon pooled URL on Vercel)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/latency_db

# Production site URL (canonical host for share links)
FRONTEND_URL=http://localhost:3000
# CORS: comma-separated browser origins (include apex + www in production)
# ALLOWED_ORIGINS=https://www.yourdomain.dev,https://yourdomain.dev

# Web dashboard limits (server-enforced; UI uses same defaults)
WEB_DEFAULT_REQUEST_COUNT=15
WEB_MAX_REQUEST_COUNT=20

# Per-request timeout when hitting target APIs (lower on Vercel Hobby)
REQUEST_TIMEOUT_MS=15000
DEFAULT_REQUEST_COUNT=15

# Rate limiting (soft, in-memory on serverless)
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW_MS=3600000

# Optional: require key for CLI --share uploads (/api/runs/import)
# IMPORT_API_KEY=
# CLI: DELAYT_IMPORT_API_KEY=<same value>

# Vercel production: set DATABASE_URL to Neon, FRONTEND_URL to https://www.delayt.foo
# See docs/vercel.md
